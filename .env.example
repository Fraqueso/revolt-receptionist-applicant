# API Configuration
REACT_APP_API_URL=http://localhost:3001

# n8n Webhook URL (for backend server)
# Replace with your actual webhook URL from n8n workflow
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/your-webhook-id

# Server Port (optional, defaults to 3001)
PORT=3001

# Optional: API Key for extra security (generate a random 32+ character string)
# API_KEY=your-secret-api-key-here

# Optional: CORS Origins (comma-separated list of allowed domains)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
